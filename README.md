# qr-code-generator

Python app which will generate a QR code, taking two inputs: the file name and the content of the QR code itself.

## Additional improvements:
- Using advanced config in the QRCode class as listed [here](https://pypi.org/project/qrcode/)
- Using Regex to check the file name is formatted correctly, e.g. no spaces in the file name
